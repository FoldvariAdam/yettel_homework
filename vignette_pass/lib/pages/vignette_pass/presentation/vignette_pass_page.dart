import 'package:flutter/material.dart';

class VignettePassPage extends StatelessWidget {
  const VignettePassPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
